{
  "name": "plannerxd-functions",
  "description": "Cloud Functions for PlannerXD - server-side, per-person Google Calendar OAuth (shared-visibility calendar + JOBSMETRIX schedule conflict checking)",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.0.0",
    "googleapis": "^140.0.0"
  },
  "private": true
}
